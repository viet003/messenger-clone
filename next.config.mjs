/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['res.cloudinary.com','avatars.githubusercontent.com'],  // Thêm tên miền Cloudinary
    },
};

export default nextConfig;
